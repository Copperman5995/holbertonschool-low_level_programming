/**
 * _islower - dfhjgdsbjfjdfnjgjubh
 * @c: c is a ASCII charter
 * Return: return 1 if loercase 0 if not
 */
#include "main.h"

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
