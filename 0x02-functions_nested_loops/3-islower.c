/**
 * main - dfhjgdsbjfjdfnjgjubh
 * Return: return 1 if loercase 0 if not
 */
#include "main.h"
int _islower(int c)
{
	char il = '1';
	char nl = '0';
	if ( _islower(c)  < 0)
	{
		_putchar(il);
		return (1);
	}
	else
	{
		_putchar(nl);
		return (0);
	}
}
