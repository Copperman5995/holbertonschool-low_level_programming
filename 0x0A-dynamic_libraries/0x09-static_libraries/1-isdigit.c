#include "main.h"
/**
 * _isdigit - dfjsddffsdfg
 * @c: c is a int
 * Return: returns 1 if c is a number 0-9 return o if not
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
