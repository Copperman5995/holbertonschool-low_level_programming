#include "main.h"
/**
 * _strlen - ajdfsgdfjjgjfdjfjgskdfk
 * @s: gggghdsgagerget
 * Return: returns int
 */

int _strlen(char *s)
{
	char l[] = (char)*s;

	int i;

	for (i = 0; l[i] != '\0'; i++)
	{
	}
	return (i);
}

