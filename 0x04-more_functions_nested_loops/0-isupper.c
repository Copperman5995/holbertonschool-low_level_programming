#include "main.h"
/**
 * _isupper - ghghgjgjdjdjdjddjjd
 * @c: c is a ASCII chater
 * Returns: return 1 if c is uppercase if not return 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
