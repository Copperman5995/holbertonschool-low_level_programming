#include "main.h"
/**
 * flip_bits - sdfgdfg
 * @n: sdfgdfsg
 * @m: sdfgdf
 * Return: sdfgdfg
 */

unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	int flip = 0;
	unsigned long int yeet = n ^ m;

	while (yeet > 0)
	{
		fipl++;
		yeet &= (num -1);
	}

	return (flip);
}
